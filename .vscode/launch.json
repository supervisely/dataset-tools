{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Local debug",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/dataset_tools/statistics/object_size_stats.py",
            "cwd": "${workspaceFolder}"
        }
    ]
}
